apiVersion: v1
kind: Service
metadata:
  name: {{ include "academy-api.fullname" . }}
  labels:
    {{- include "academy-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "academy-api.selectorLabels" . | nindent 4 }}
